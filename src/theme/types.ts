import { ThemeSettings } from "@/theme/settings/SettingsProvider";

// ----------------------------------------------------------------------

export const defaultSetting: ThemeSettings = {
  colorScheme: "light",
  compactLayout: true,
  navLayout: "vertical",
};
